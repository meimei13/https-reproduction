{
  "name": "https-reproduction",
  "version": "1.0.0",
  "description": "When calling https.get(url[,options]) node will throw an error",
  "main": "test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "agent-base": "^4.3.0",
    "https": "^1.0.0",
    "url": "^0.11.0",
  }
}
